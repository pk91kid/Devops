apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
spec:
  selector:
    app: jenkins
  ports:
    - protocol: TCP
      port: 8081  # Expose Jenkins service on port 8081
      targetPort: 8081  # The internal port that the Jenkins container is listening on
  type: NodePort
